var exec = require('child_process').exec;
var path = require('path');

module.exports = cons;

function cons(target, callback) {
  var opener;

  opener = '/usr/bin/console';
  return exec(opener + ' "' + escape(target) + '"', callback);
}

function escape(s) {
  return s.replace(/"/, '\\\"');
}
