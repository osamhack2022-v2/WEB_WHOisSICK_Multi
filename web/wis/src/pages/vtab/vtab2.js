import { Container } from '@mui/system';
import { Box, Typography } from '@mui/material';
import * as React from 'react';

export default function Vtab2() {
    return (
        <Container component="main" maxWidth>
             <Box
            sx={{ 
                 
            }}
            >
                <Typography>TesTestTest</Typography>
            </Box>
        </Container>
    );
}